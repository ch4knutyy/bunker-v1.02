// Canonical game-world values shared by client-side derived presentation.
// This is intentionally static game configuration, not a live-world statistic.
const worldPopulationSettings = Object.freeze({
	beforeApocalypse: 8_200_000_000
});
